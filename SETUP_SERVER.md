# Настройка сервера для деплоя (Beget Shared Hosting)

## ⚡ Быстрый старт

**Для быстрой настройки выполните на сервере:**

```bash
# 1. Установка Composer (используйте php8.2 если требуется)
cd ~ && php8.2 -r "copy('https://getcomposer.org/installer', 'composer-setup.php');" && php8.2 composer-setup.php && rm composer-setup.php
mkdir -p ~/bin && mv composer.phar ~/bin/composer && chmod +x ~/bin/composer
echo 'export PATH="$HOME/bin:$PATH"' >> ~/.bashrc

# 2. Установка nvm и Node.js
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
echo 'export NVM_DIR="$HOME/.nvm"' >> ~/.bashrc
echo '[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"' >> ~/.bashrc
source ~/.bashrc
nvm install --lts && nvm use --lts && nvm alias default node

# 3. Переподключитесь по SSH для применения изменений
```

**Подробная инструкция:** см. `SERVER_SETUP.md`

---

## Описание

Эта инструкция настраивает Composer и npm на shared-хостинге Beget без root-доступа, чтобы команда `php artisan deploy` работала корректно.

## 🔹 1. Установка Composer (глобально для пользователя)

### Шаг 1. Подключение по SSH

В панели Beget:
- **Хостинг → SSH → Включить SSH**

Подключение:
```bash
ssh login@server.beget.com
```

### Шаг 2. Скачивание Composer

```bash
cd ~
php8.2 -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
php8.2 composer-setup.php
rm composer-setup.php
```

**Примечание:** Если на вашем сервере используется другая версия PHP (например, `php8.1` или `php`), замените `php8.2` на соответствующую команду.

Появится файл: `~/composer.phar`

### Шаг 3. Создание глобальной команды composer

```bash
# Создаём папку для пользовательских бинарников
mkdir -p ~/bin
mv composer.phar ~/bin/composer
chmod +x ~/bin/composer
```

### Шаг 4. Добавление ~/bin в PATH

Открой `.bashrc`:
```bash
nano ~/.bashrc
```

Добавь в конец файла:
```bash
export PATH="$HOME/bin:$PATH"
```

Применяем:
```bash
source ~/.bashrc
```

### ✅ Проверка Composer

```bash
composer -V
```

Должен вывести версию Composer.

---

## 🔹 2. Установка Node.js + npm (через nvm)

Beget НЕ позволяет ставить Node глобально через apt — используем nvm.

### Шаг 1. Установка nvm

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
```

Применяем:
```bash
source ~/.bashrc
```

### Шаг 2. Установка Node.js (LTS версия)

```bash
nvm install --lts
nvm use --lts
```

### Шаг 3. Проверка

```bash
node -v
npm -v
```

### Шаг 4. Настройка npm PATH (если команды не видны)

Проверяем путь npm:
```bash
npm config get prefix
```

Обычно: `/home/login/.nvm/versions/node/vXX.X.X`

Добавь в `.bashrc`:
```bash
export PATH="$PATH:$(npm config get prefix)/bin"
```

Применяем:
```bash
source ~/.bashrc
```

---

## 🔹 3. Глобальные npm-пакеты (без sudo)

npm по умолчанию будет писать в домашнюю директорию — проблем с правами не будет.

Пример установки глобальных пакетов (опционально):
```bash
npm install -g pm2
npm install -g vite
```

---

## 🔹 4. Итоговая структура

```
/home/login/
 ├── bin/
 │   └── composer          # Глобальный Composer
 ├── .nvm/                 # Node Version Manager
 │   └── versions/
 │       └── node/
 │           └── vXX.X.X/
 ├── .bashrc               # Конфигурация shell
 └── www/
     └── project/          # Ваш Laravel проект
```

---

## 🔹 5. Проверка настройки

После настройки выполните:

```bash
# Проверка Composer
composer -V

# Проверка Node.js
node -v

# Проверка npm
npm -v

# Проверка путей
which composer
which node
which npm
```

Все команды должны работать из любого каталога.

---

## 🔹 6. Настройка для автоматического деплоя

Команда `php artisan deploy` автоматически определит пути к:
- `composer` (из `~/bin/composer` или системный)
- `npm` (из `~/.nvm/versions/node/*/bin/npm` или системный)

Если нужно указать пути вручную, добавьте в `.env`:

```env
# Пути к инструментам (опционально, определяется автоматически)
COMPOSER_PATH=/home/login/bin/composer
NPM_PATH=/home/login/.nvm/versions/node/vXX.X.X/bin/npm
NODE_PATH=/home/login/.nvm/versions/node/vXX.X.X/bin/node
```

---

## 🔹 7. Автоматическая настройка (скрипт)

Для автоматической настройки выполните на сервере:

```bash
cd ~
curl -o setup-server.sh https://raw.githubusercontent.com/your-repo/setup-server.sh
chmod +x setup-server.sh
./setup-server.sh
```

Или используйте локальный скрипт `scripts/setup-server.sh` (см. ниже).

---

## ⚠️ Важные замечания

1. **После настройки переподключитесь по SSH** для применения изменений PATH
2. **Проверьте права доступа** к `~/bin` и `~/.nvm`
3. **Для cron-задач** используйте полные пути или настройте PATH в cron
4. **При обновлении Node.js через nvm** обновите пути в `.env` если используете

---

## 🔧 Устранение проблем

### Composer не найден

```bash
# Проверьте наличие файла
ls -la ~/bin/composer

# Проверьте PATH
echo $PATH

# Добавьте вручную в текущей сессии
export PATH="$HOME/bin:$PATH"
```

### npm не найден

```bash
# Проверьте установку nvm
ls -la ~/.nvm

# Проверьте активную версию Node
nvm current

# Активируйте версию
nvm use --lts

# Проверьте путь npm
which npm
```

### Проблемы с правами

```bash
# Убедитесь, что директории принадлежат вам
chown -R $USER:$USER ~/bin ~/.nvm

# Проверьте права на исполнение
chmod +x ~/bin/composer
```

